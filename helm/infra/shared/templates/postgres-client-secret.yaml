apiVersion: v1
kind: Secret

metadata:
  name: postgres-secret
  namespace: {{ .Values.namespace}}

type: Opaque

stringData:
  username: {{ .Values.postgres.username }}
  password: {{ .Values.postgres.password }}
  # not used directly, but might be useful in future
  database: {{ .Values.postgres.database }}